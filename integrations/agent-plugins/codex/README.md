# koment for Codex

This directory contains the Codex host package. The installable plugin is in
[`plugins/koment/`](plugins/koment/).

This directory is a valid local Codex marketplace. Release packaging signs the
complete marketplace archive. The generated repository adapter remains
available through `koment agents install`.
