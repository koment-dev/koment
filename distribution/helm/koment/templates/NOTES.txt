koment serves its authenticated UI and MCP endpoint on port {{ .Values.service.port }}.

  kubectl --namespace {{ .Release.Namespace }} port-forward svc/{{ include "koment.fullname" . }} {{ .Values.service.port }}:{{ .Values.service.port }}

Health endpoints:
  /livez
  /readyz

{{- if and (not .Values.auth.existingSecret) (eq (join "," .Values.auth.trustedProxies) "127.0.0.1/32") }}
Only a loopback trusted proxy can assert human identity with the default auth
configuration. Configure auth.existingSecret for agents or trustedProxies for
your OIDC proxy before exposing the service.
{{- end }}
