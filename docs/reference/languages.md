# Language support

<!-- Generated by internal/commentpolicy/gen. Edit the syntax table, not this page. -->

koment does two different things to a file, and they have different reach.

**Anchoring** binds an annotation to a snippet of text. It is a verbatim search
with surrounding context, so it has no idea what language it is looking at and
works in every file in the repository.

**Comment detection** finds the comments already in a file so koment can flag
them, convert them, or offer to. It has to know where a comment ends and a
string begins, so it reads the file's syntax.

Everything below is about the second one.

## Which detector reads a file

| detector | files | what it knows |
|---|---|---|
| `go/ast` | `*.go` | comment groups, and godoc on exported identifiers |
| `marker scan` | everything else | the line and block markers listed below |

The Go parser is tried first, because no marker scan can tell godoc on an
exported identifier from ordinary prose.

## What detection gives you

- `koment comments check` — the gate that fails when a prohibited comment lands
- `koment comments convert` — move a comment into an annotation and delete it
- `koment comments acknowledge` — keep a comment, attributably
- the editor prompt that offers to convert a comment as you finish typing it

These work in every filetype below. Only the formats in
[Never scanned](#never-scanned) report `no comment detector for <file>`.

## Filetypes the scan names

A **directive** is a prefix koment passes through untouched, because the
toolchain requires it rather than a person explaining something.

| extension | line | block | directives |
|---|---|---|---|
| `.bash` | `#` | — | `shellcheck` `!` |
| `.c` | `//` | `/* … */` | `NOLINT` `clang-format` `cppcheck-suppress` |
| `.cc` | `//` | `/* … */` | `NOLINT` `clang-format` `cppcheck-suppress` |
| `.cfg` | `#` | — | — |
| `.cjs` | `//` | `/* … */` | `eslint` `@ts-` `prettier-` |
| `.conf` | `#` | — | — |
| `.cpp` | `//` | `/* … */` | `NOLINT` `clang-format` `cppcheck-suppress` |
| `.cs` | `//` | `/* … */` | — |
| `.css` | — | `/* … */` | — |
| `.cxx` | `//` | `/* … */` | `NOLINT` `clang-format` |
| `.dart` | `//` | `/* … */` | `ignore:` `ignore_for_file:` |
| `.env` | `#` | — | — |
| `.ex` | `#` | — | — |
| `.fish` | `#` | — | — |
| `.go2` | `//` | `/* … */` | — |
| `.h` | `//` | `/* … */` | `NOLINT` `clang-format` `cppcheck-suppress` |
| `.hcl` | `#` | — | — |
| `.hh` | `//` | `/* … */` | `NOLINT` `clang-format` |
| `.hpp` | `//` | `/* … */` | `NOLINT` `clang-format` |
| `.hs` | `--` | `{- … -}` | — |
| `.ini` | `#` | — | — |
| `.java` | `//` | `/* … */` | `CHECKSTYLE` `NOPMD` `SUPPRESS` |
| `.js` | `//` | `/* … */` | `eslint` `@ts-` `prettier-` `istanbul ` `jshint` `global ` `jsx` |
| `.jsonc` | `//` | `/* … */` | — |
| `.jsx` | `//` | `/* … */` | `eslint` `@ts-` `prettier-` |
| `.kt` | `//` | `/* … */` | `ktlint-` `noinspection` |
| `.kts` | `//` | `/* … */` | — |
| `.less` | `//` | `/* … */` | — |
| `.lua` | `--` | `--[[ … ]]` | `luacheck:` `@` |
| `.mjs` | `//` | `/* … */` | `eslint` `@ts-` `prettier-` |
| `.mod` | `//` | `/* … */` | `indirect` |
| `.php` | `//` | `/* … */` | `phpcs:` `@phpstan-` |
| `.pl` | `#` | — | — |
| `.proto` | `//` | `/* … */` | — |
| `.py` | `#` | — | `type:` `noqa` `pylint:` `mypy:` `ruff:` `fmt:` `pragma:` `-*-` |
| `.pyi` | `#` | — | `type:` `noqa` `pylint:` |
| `.r` | `#` | — | — |
| `.rb` | `#` | — | `frozen_string_literal:` `rubocop:` `encoding:` |
| `.rs` | `//` | `/* … */` | `!` `/` `rustfmt::` `clippy::` `allow(` `cfg(` |
| `.scala` | `//` | `/* … */` | — |
| `.scss` | `//` | `/* … */` | — |
| `.sh` | `#` | — | `shellcheck` `!` |
| `.sql` | `--` | `/* … */` | `noqa` |
| `.swift` | `//` | `/* … */` | `swiftlint:` `swift-format-` |
| `.tf` | `#` | — | — |
| `.toml` | `#` | — | `schema:` `renovate:` `x-release-please` |
| `.ts` | `//` | `/* … */` | `eslint` `@ts-` `prettier-` `tslint:` `/ <reference` `istanbul ` |
| `.tsx` | `//` | `/* … */` | `eslint` `@ts-` `prettier-` `tslint:` |
| `.vim` | `"` | — | — |
| `.yaml` | `#` | — | `yaml-language-server:` `yamllint` `renovate:` `noqa` `zizmor:` `x-release-please` `checkov:` `kubeconform` `helm-docs` |
| `.yml` | `#` | — | `yaml-language-server:` `yamllint` `renovate:` `noqa` `zizmor:` `x-release-please` `checkov:` `kubeconform` `helm-docs` |
| `.zig` | `//` | `/* … */` | — |
| `.zsh` | `#` | — | `shellcheck` `!` |

## Anything else

An extension this page does not name is still scanned, using `#` `//`.

`--` is deliberately absent from that fallback: it opens a comment in Lua, SQL and Haskell, which declare it above, and separates YAML documents everywhere else.

These extensionless filenames are read as shell:

`brewfile` `dockerfile` `gemfile` `justfile` `makefile` `rakefile` `vagrantfile`

So is any dotfile whose name carries no further extension — `.gitignore`, `.editorconfig`.

## Never scanned

Prose and data formats carry no comment syntax to find, and scanning them would read every Markdown heading as a violation:

`.adoc` `.csv` `.htm` `.html` `.json` `.lock` `.markdown` `.md` `.mdc` `.mdx` `.rst` `.sum` `.svg` `.tsv` `.txt` `.xml`

Annotate them freely; anchoring does not care.
