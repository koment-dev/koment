# yaml-language-server: $schema=https://raw.githubusercontent.com/koment-dev/koment/main/schema/v1alpha/annotation.schema.json
apiVersion: koment.dev/v1alpha
kind: Annotation
metadata:
  id: 01KZ5NZT0H94223K8AR010TK6X
  created: "2026-08-04T00:00:00Z"
spec:
  target:
    file: integrations/editors/vscode/binary.js
  type: why
  title: Chmod is asserted; a VSIX expansion is not guaranteed executable
  body: |-
    A VSIX is a zip and nothing in the extension host contract guarantees the
    executable bit survives expansion. It does survive on the paths tested here,
    but a server that cannot be executed fails exactly like a server that is
    missing, so the bit is asserted rather than assumed. A chmod failure is
    deliberately not caught; swallowing it would restore the failure this
    prevents.
  anchor:
    scope: excerpt
    excerpt: function ensureExecutable(binary, { platform = process.platform, stat = fs.statSync, chmod = fs.chmodSync } = {}) {
    before: |2
        return { command: 'koment', source: 'path' };
      }
    after: |2-
        if (platform === 'win32') {
          return false;
        }
  author:
    name: janpuc
    email: janpuc@proton.me
    kind: agent
    source: git-config
status:
  lastSeenLine: 26
