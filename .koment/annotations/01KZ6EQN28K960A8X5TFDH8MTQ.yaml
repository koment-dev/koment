# yaml-language-server: $schema=https://raw.githubusercontent.com/koment-dev/koment/main/schema/v1alpha/annotation.schema.json
apiVersion: koment.dev/v1alpha
kind: Annotation
metadata:
  id: 01KZ6EQN28K960A8X5TFDH8MTQ
  created: "2026-08-04T00:00:00Z"
spec:
  target:
    file: integrations/editors/vscode/panel.js
  type: why
  title: Annotation prose goes through escape; webview loads nothing external
  body: |-
    Annotation bodies are prose written by whoever edits the repository, and
    this renders them into a document with scripting enabled. Everything
    reaching the markup goes through here first. The webview also declares
    default-src none with per-render nonces, so even an escape that missed
    something cannot load or run anything external.
  anchor:
    scope: excerpt
    excerpt: function escape(text) {
    before: |2
        orphaned: 'failing'
      };
    after: |2-
        return String(text ?? '').replace(
          /[&<>"']/g,
          (character) => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' })[character]
  author:
    name: janpuc
    email: janpuc@proton.me
    kind: agent
    source: git-config
status:
  lastSeenLine: 11
