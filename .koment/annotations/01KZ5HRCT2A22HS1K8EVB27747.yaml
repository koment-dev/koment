# yaml-language-server: $schema=https://raw.githubusercontent.com/koment-dev/koment/main/schema/v1alpha/annotation.schema.json
apiVersion: koment.dev/v1alpha
kind: Annotation
metadata:
  id: 01KZ5HRCT2A22HS1K8EVB27747
  created: "2026-08-04T00:00:00Z"
spec:
  target:
    file: distribution/package-managers/naming_test.go
  type: invariant
  title: The release workflow names the files every channel may reference
  body: |-
    The release workflow is the producer, so it defines the names every package
    manager is allowed to reference (ADR 0109).
  anchor:
    scope: excerpt
    excerpt: func published(t *testing.T) map[archive]bool {
    before: |2

      const releaseWorkflow = ".github/workflows/release.yml"
    after: "\tt.Helper()\n\tworkflow := repositoryFile(t, releaseWorkflow)\n"
  author:
    name: janpuc
    email: janpuc@proton.me
    kind: agent
    source: git-config
status:
  lastSeenLine: 55
