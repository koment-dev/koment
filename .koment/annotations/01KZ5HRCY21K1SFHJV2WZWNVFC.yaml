# yaml-language-server: $schema=https://raw.githubusercontent.com/koment-dev/koment/main/schema/v1alpha/annotation.schema.json
apiVersion: koment.dev/v1alpha
kind: Annotation
metadata:
  id: 01KZ5HRCY21K1SFHJV2WZWNVFC
  created: "2026-08-04T00:00:00Z"
spec:
  target:
    file: distribution/package-managers/naming_test.go
  type: gotcha
  title: Each package manager derives its URL from the archive name
  body: |-
    Each consumer derives its download URL from the archive name, so a rename in
    the release workflow breaks them silently and only after a release ships.
  anchor:
    scope: excerpt
    excerpt: var consumers = map[string]string{
    before: "\treturn \"tar.gz\"\n}\n"
    after: "\t\"the Homebrew tap\":  \"distribution/package-managers/homebrew/koment.rb.tmpl\",\n\t\"the Scoop bucket\":  \"distribution/package-managers/scoop/koment.json.tmpl\",\n\t\"the WinGet bundle\": \"distribution/package-managers/winget/Koment.Koment.installer.yaml.tmpl\","
  author:
    name: janpuc
    email: janpuc@proton.me
    kind: agent
    source: git-config
status:
  lastSeenLine: 89
