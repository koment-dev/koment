# yaml-language-server: $schema=https://raw.githubusercontent.com/koment-dev/koment/main/schema/v1alpha/annotation.schema.json
apiVersion: koment.dev/v1alpha
kind: Annotation
metadata:
  id: 01KZ5NZT45QNB74R1J4YTYEBQS
  created: "2026-08-04T00:00:00Z"
spec:
  target:
    file: integrations/editors/vscode/package-vsix.sh
  type: invariant
  title: Bundled binary is the archive the release signed, never a rebuild
  body: |-
    The bundled binary must be the archive the release signed, never a rebuild.
    ADR 0109 rejected letting a channel build its own binary, because that lets
    one channel ship something never signed or attested, and a marketplace
    extension is a channel. This is why the release job depends on the binaries
    job and downloads its output instead of running go build itself.
  anchor:
    scope: excerpt
    excerpt: verify() {
    before: |
      cp "$repository/LICENSE" "$extension/LICENSE"
      cp "$repository/internal/ui/assets/koment-logo.png" "$extension/icon.png"
    after: |2-
        local archive=$1
        (
          cd "$archives"
  author:
    name: janpuc
    email: janpuc@proton.me
    kind: agent
    source: git-config
status:
  lastSeenLine: 25
