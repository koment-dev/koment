# yaml-language-server: $schema=https://raw.githubusercontent.com/koment-dev/koment/main/schema/v1alpha/annotation.schema.json
apiVersion: koment.dev/v1alpha
kind: Annotation
metadata:
  id: 01KZ2Z70F4D6MMSTAVRGEVE0X1
  created: "2026-08-03T00:00:00Z"
spec:
  target:
    file: internal/ui/assets/koment.js
  type: invariant
  title: Note floor only moves down; code stays still when notes collide
  body: |-
    The floor only ever moves down, which is what guarantees the property this
    layout exists for: when two notes would overlap, the note moves and the code
    never does. Placing a note at max(wanted, floor) can leave it below its
    line, and that is the intended trade — a note slightly out of line is
    readable, code pushed apart by a tall note is not.
  anchor:
    scope: excerpt
    excerpt: '        floor = placed + note.offsetHeight + noteGap;'
    before: |2-
              var placed = Math.max(wanted, floor);

              note.style.transform = "translateY(" + (placed - resting) + "px)";
    after: |2-
            });

            gloss.style.height = floor + "px";
  author:
    name: janpuc
    email: janpuc@proton.me
    kind: agent
    source: git-config
status:
  lastSeenLine: 227
  resolution: ok
  resolvedAt: "2026-08-19T11:58:38Z"
  resolvedCommit: c44fa3fe356b82bd28e785f1387df8b4a62ab91f
