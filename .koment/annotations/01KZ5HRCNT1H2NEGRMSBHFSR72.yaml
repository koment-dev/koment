# yaml-language-server: $schema=https://raw.githubusercontent.com/koment-dev/koment/main/schema/v1alpha/annotation.schema.json
apiVersion: koment.dev/v1alpha
kind: Annotation
metadata:
  id: 01KZ5HRCNT1H2NEGRMSBHFSR72
  created: "2026-08-04T00:00:00Z"
spec:
  target:
    file: distribution/package-managers/naming_test.go
  type: why
  title: Versions are tokenised so every channel can be compared by name
  body: |-
    Every channel spells the release version differently; comparing names is
    only possible once they all say the same thing.
  anchor:
    scope: excerpt
    excerpt: var versionTokens = strings.NewReplacer(
    before: "\tchecksumMention = regexp.MustCompile(`koment_` + versionPlaceholder + `_checksums\\.txt`)\n)\n"
    after: "\t\"${VERSION}\", versionPlaceholder,\n\t\"{{VERSION}}\", versionPlaceholder,\n\t\"${version}\", versionPlaceholder,"
  author:
    name: janpuc
    email: janpuc@proton.me
    kind: agent
    source: git-config
status:
  lastSeenLine: 22
