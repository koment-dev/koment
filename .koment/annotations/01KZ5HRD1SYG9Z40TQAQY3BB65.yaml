# yaml-language-server: $schema=https://raw.githubusercontent.com/koment-dev/koment/main/schema/v1alpha/annotation.schema.json
apiVersion: koment.dev/v1alpha
kind: Annotation
metadata:
  id: 01KZ5HRD1SYG9Z40TQAQY3BB65
  created: "2026-08-04T00:00:00Z"
spec:
  target:
    file: distribution/package-managers/naming_test.go
  type: why
  title: Setup action maps runners onto archive names, not the other way
  body: |-
    The setup action names no archive literally: it maps the runner it finds
    onto one. Expanding that mapping is the only way to compare it with what
    ships.
  anchor:
    scope: excerpt
    excerpt: func requestedByTheSetupAction(t *testing.T) map[archive]bool {
    before: "\tactionArchitecure = regexp.MustCompile(`arch=([a-z0-9]+)\\s*;;`)\n)\n"
    after: "\tt.Helper()\n\tcontent := repositoryFile(t, setupAction)\n"
  author:
    name: janpuc
    email: janpuc@proton.me
    kind: agent
    source: git-config
status:
  lastSeenLine: 103
